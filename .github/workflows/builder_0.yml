name: Builder 0
run-name: "${{ github.workflow }} triggered by ${{ github.triggering_actor }}"

on:
  push:
    branches:
      - 'develop*'
  workflow_dispatch:

jobs:
  builder:
    name: Build application's Variant-0 
    needs:
      - get-build-number
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-tags: true
        
      - name: Show environment
        run: env

      - name: Dump env context
        env:
          ENV_CONTEXT: ${{ toJson(env) }}
        run: echo "$ENV_CONTEXT"
        
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
        
      - name: Dump runner context
        env:
          RUNNER_CONTEXT: ${{ toJson(runner) }}
        run: echo "$RUNNER_CONTEXT"
        
      - name: Dump needs outputs
        env:
          OUTPUT_CONTEXT: ${{ toJson(needs) }}
        run: echo "$OUTPUT_CONTEXT"
        
      - name: Dump inputs context
        env:
          INPUTS_CONTEXT: ${{ toJson(inputs) }}
        run: echo "$INPUTS_CONTEXT"
      
      - name: Show build number
        run: |
          echo Build Number: ${{ needs.get-build-number.outputs.build_number }}
          
#          PATH=/snap/bin:
#          /home/runner/.local/bin:
#          /opt/pipx_bin:/home/runner/.cargo/bin:
#          /home/runner/.config/composer/vendor/bin:
#          /usr/local/.ghcup/bin:
#          /home/runner/.dotnet/tools:
#          /usr/local/sbin:
#          /usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
#               find $ANDROID_NDK_LATEST_HOME -type f
      - name: Install Android SDK
        run: |
          sudo apt-get -q update && sudo apt-get -q install -y android-sdk
          sudo ln -s /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager ~/.local/bin/sdkmanager
          sdkmanager --install "build-tools;34.0.0"        
        
  get-build-number:
    name: Retrieve build number
    needs: 
      - get-package-version
    uses: ./.github/workflows/build-number.yml
    with:
      prefix: "${{ needs.get-package-version.outputs.packageVersion }}/"

  get-package-version:
    name: Retrieve package version 
    runs-on: ubuntu-latest
    outputs: 
      packageVersion: ${{ steps.read-file.outputs.packageVersion }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
  
      - name: Read base version from project file
        id: read-file
        run: |
          PACKAGE_VERSION=$(grep "<PackageVersion>" ./workflow-sandbox/workflow-sandbox.csproj | sed "s@.*<PackageVersion>\(.*\)</PackageVersion>.*@\1@")
          echo "PackageVersion: ${PACKAGE_VERSION}" 
          echo "packageVersion=$PACKAGE_VERSION" >> "$GITHUB_OUTPUT"

